{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install --production",
    "deploy": "npm run build && pm2 start server.js --name cyber-guard-api"
  }
}